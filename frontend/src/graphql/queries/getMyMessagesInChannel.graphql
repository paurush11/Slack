query GetMyMessagesInChannel($channelId: String!) {
  getMyMessagesInChannel(channelId: $channelId) {
    _id
    sender {
      _id
      lastName
      firstName
      isActive
      username
    }
    receiver {
       _id
      lastName
      firstName
      isActive
      username
    }
  }
}